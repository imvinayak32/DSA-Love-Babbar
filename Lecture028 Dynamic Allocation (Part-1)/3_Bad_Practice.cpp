#include<bits/stdc++.h>
using namespace std;

int main(){

    int n;
    cin>>n;
    int arr[n];

    // Segmentation Fault may occur due runtime excess stack memory allocation which has resulted in overflow.

    return 0;
}